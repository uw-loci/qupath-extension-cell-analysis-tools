# Moved

This page is now **[Batch runs > When it fails](batch.md#when-it-fails)**.

The documentation is one page per topic; start at
**[documentation/README.md](README.md)** to find anything else.

> This stub exists because installed copies of the extension link here. The
> in-app "Documentation" buttons of an older build point at this path, so
> deleting the file outright would 404 for anyone who has not updated.
